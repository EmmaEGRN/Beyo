using TMPro;
using UnityEngine;

public class VidasUIHandler : MonoBehaviour
{

    TMP_Text numerodeVidas;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        //numerodeVidas.text = LiveManager.instance.liveCounter.toString;        
    }

}
